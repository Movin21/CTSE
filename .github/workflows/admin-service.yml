name: Admin CI/CD

on:
  push:
    branches: [main]
    paths: ['admin-service/**']
  pull_request:
    branches: [main]
    paths: ['admin-service/**']
  workflow_dispatch:

permissions:
  security-events: write

jobs:
  deploy:
    uses: ./.github/workflows/service-ci-cd.yml
    with:
      service: "admin-service"
      language: "node"
      port: 3003
      ingress: "internal"
    secrets: inherit
