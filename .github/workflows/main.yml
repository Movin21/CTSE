name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  ci-java:
    name: CI - Java Services
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [identity-service, product-service]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build and Test (${{ matrix.service }})
        working-directory: ./${{ matrix.service }}
        run: mvn clean test

      - name: Snyk Security Scan (${{ matrix.service }})
        uses: snyk/actions/maven@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args: --all-projects
        continue-on-error: true

  ci-node:
    name: CI - Node.js Services
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [order-service, notification-service, admin-service, api-gateway, frontend]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        working-directory: ./${{ matrix.service }}
        run: npm ci || npm install

      - name: Run Tests (${{ matrix.service }})
        working-directory: ./${{ matrix.service }}
        run: npm test
        env:
          CI: true

      - name: Snyk Security Scan (${{ matrix.service }})
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
        continue-on-error: true

  docker-build-push:
    name: Docker Build & Push
    needs: [ci-java, ci-node]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: [identity-service, product-service, order-service, notification-service, admin-service, api-gateway, frontend]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GIT_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/${{ matrix.service }}
          tags: |
            type=sha,format=long

      - name: Build and Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.service }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy-azure:
    name: CD - Deploy to Azure Container Apps
    needs: docker-build-push
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment: ${{ matrix.service }}
    strategy:
      matrix:
        include:
          - service: identity-service
            port: 8081
            ingress: internal
          - service: product-service
            port: 8082
            ingress: internal
          - service: order-service
            port: 3001
            ingress: internal
          - service: notification-service
            port: 3002
            ingress: internal
          - service: admin-service
            port: 3003
            ingress: internal
          - service: api-gateway
            port: 8080
            ingress: external
          - service: frontend
            port: 80
            ingress: external
    steps:
      - name: Log in to Azure
        run: |
          az login --service-principal \
            -u ${{ secrets.ARM_CLIENT_ID }} \
            -p ${{ secrets.ARM_CLIENT_SECRET }} \
            --tenant ${{ secrets.ARM_TENANT_ID }}
          az account set --subscription ${{ secrets.ARM_SUBSCRIPTION_ID }}

      - name: Deploy to Azure Container Apps
        uses: azure/container-apps-deploy-action@v2
        with:
          imageToDeploy: ghcr.io/${{ github.repository }}/${{ matrix.service }}:sha-${{ github.sha }}
          containerAppName: ${{ matrix.service }}
          resourceGroup: ${{ secrets.AZURE_RESOURCE_GROUP }}
          containerAppEnvironment: ${{ secrets.AZURE_CONTAINER_APP_ENV }}
          targetPort: ${{ matrix.port }}
          ingress: ${{ matrix.ingress }}
          registryUrl: ghcr.io
          registryUsername: ${{ github.actor }}
          registryPassword: ${{ secrets.GIT_TOKEN }}
          environmentVariables: |
            DATABASE_URL=${{ secrets.DB_URL }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            RABBITMQ_URL=${{ secrets.RABBITMQ_URL }}
            DATABASE_USER=${{ secrets.POSTGRES_USER }}
            DATABASE_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
            ORDER_SERVICE_URL=http://order-service
            IDENTITY_SERVICE_URL=http://identity-service
            PRODUCT_SERVICE_URL=http://product-service
            NOTIFICATION_SERVICE_URL=http://notification-service
            ADMIN_SERVICE_URL=http://admin-service
